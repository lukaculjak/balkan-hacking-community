import styled from "styled-components";

const StyledLogin = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;

  height: 100vh;
  background-color: red;

  background: linear-gradient(
    120deg,
    var(--color-brand-10) 0%,
    var(--color-brand-20) 100%
  );

  padding-top: 10rem;
`;

export default StyledLogin;
